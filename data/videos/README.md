# Demo Video Fixtures

Drop local MP4 files here using the names referenced in `data/scenarios.json`.

The backend works without these binaries by producing timestamped placeholder
frame references. Real clips improve live VLM quality, but tests use the mock
adapter and do not require video files.
